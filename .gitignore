
# Node modules
node_modules/

# Bun lockfile
bun.lockb
bun.lock

# Ignore a stray root-level favicon.ico artifact (NOT public/assets/favicon.ico)
/favicon.ico

# output
out
dist
dist-linux
*.tgz
*.tar.gz

# executável: binários compilados e zips de entrega
*.exe
*.zip

# Scratchpad de notas/reviews (não é código do repo)
claude.md

# code coverage
coverage
*.lcov

# logs
logs
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
*.log

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Tiled editor session snapshots (contain local paths)
*.tiled-session

# Steamworks (SDK local, não versionar)
steam_api64.dll
steam_appid.txt
# CEF: distribuição binária + builds (~2 GB). O shell é reproduzido pelo
# tools/cef/README.md a partir de tools/cef/patches/.
tools/cef/cef/
tools/cef/*.tar.bz2
tools/cef/*.7z
# sobras de `patch` / regeneração do patch
*.rej
*.orig
tools/cef/orig/
tools/cef/norm/

# Spikes encerrados (Ultralight, Electron) — apagar quando confortável
tools/ultralight/
tools/overlay-test/

# Claude Code: permissoes locais desta maquina
.claude/settings.local.json

# Pacote Steam (exe do shell + DLLs do CEF + servidor compilado, ~480 MB)
dist-steam/
